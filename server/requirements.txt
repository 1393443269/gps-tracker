flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
simple-websocket>=0.9.0
paho-mqtt>=1.6.0
psycopg2-binary>=2.9.0
# ── 生产并发层 ─────────────────────────────────────────────────────────────────
gevent>=23.0.0               # 协程 I/O，配合 Gunicorn gevent worker
gevent-websocket>=0.10.1     # Gunicorn GeventWebSocketWorker，支持 WS 升级
gunicorn>=21.0.0             # 生产 WSGI 服务器（替换 Flask dev server）
psycogreen>=1.0.2            # psycopg2 gevent 等待回调，DB I/O 协作式非阻塞
bcrypt>=4.0.0
